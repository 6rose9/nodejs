// require("./createdirectory")
// require("./renamedirectory")
// require("./removedirectory")
// require("./checkexist")
// require("./readcontent")
// require("./directoryinfo")

//------------------------------------------------------------------------ Notes

// fs.mkdirSync(path,{recursive:true})
// fs.mkdir(path, {recursive:true}, callback(err,data))

// fs.renameSync(old path, new path)
// fs.rename(old path, new path, callback)

// fs.rmSync(path,{recursive:true, force : true});
// fs.rm(path,{recursive:true, force : true}, callback);

// fs.existsSync(file path or dir path)

// fs.readdir(path,callback())
// Note :{withFileTypes:true} return direct object 

// fs.state(path, callback())

